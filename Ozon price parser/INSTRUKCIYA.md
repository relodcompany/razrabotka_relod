# 📖 ИНСТРУКЦИЯ ПО УСТАНОВКЕ И ЗАПУСКУ ПАРСЕРА OZON

## 🎯 Что делает этот парсер?

Парсер автоматически:
1. Получает список ваших книг из магазина на Ozon через API
2. Ищет эти книги у конкурентов по ISBN и названию
3. Сравнивает цены (берет минимальную среди конкурентов)
4. Создает Excel отчет с выделением критических позиций
5. Сохраняет историю для анализа динамики
6. Отправляет уведомления о результатах

---

## 📋 ШАГ 1: УСТАНОВКА PYTHON

1. Скачайте Python 3.8 или новее с официального сайта: https://www.python.org/downloads/
2. При установке **ОБЯЗАТЕЛЬНО** поставьте галочку "Add Python to PATH"
3. Проверьте установку, открыв командную строку (cmd) и выполнив:
   ```
   python --version
   ```
   Должна отобразиться версия Python

---

## 📂 ШАГ 2: ПОДГОТОВКА ФАЙЛОВ

1. Создайте папку для проекта, например: `C:\ozon_parser`

2. Скопируйте в эту папку все файлы проекта:
   - main.py
   - config.py
   - ozon_api.py
   - parser.py
   - excel_reporter.py
   - history_manager.py
   - notifications.py
   - requirements.txt
   - .env
   - proxies.txt

3. Создайте пустые папки:
   - output/
   - history/
   - logs/

---

## 🔧 ШАГ 3: УСТАНОВКА ЗАВИСИМОСТЕЙ

1. Откройте командную строку (cmd) в папке проекта:
   - Способ 1: В проводнике откройте папку проекта, в адресной строке напишите `cmd` и нажмите Enter
   - Способ 2: Откройте cmd и выполните: `cd C:\ozon_parser`

2. Установите необходимые библиотеки:
   ```
   pip install -r requirements.txt
   ```

3. Дождитесь окончания установки (может занять несколько минут)

---

## 🔑 ШАГ 4: ПОЛУЧЕНИЕ API КЛЮЧЕЙ OZON

### Для Seller API (обязательно):

1. Откройте: https://seller.ozon.ru
2. Войдите под аккаунтом администратора
3. Нажмите на аватарку в правом верхнем углу → **Настройки**
4. В левом меню выберите **API ключи** → вкладка **Seller API**
5. Скопируйте **Client Id** (это число из цифр)
6. Нажмите **Сгенерировать ключ**:
   - Название: любое (например, "Parser")
   - Тип: **Admin** (для полного доступа) или **Admin read only** (для чтения)
   - Нажмите **Создать ключ**
7. Скопируйте созданный **API Key** (длинная строка из букв и цифр)

⚠️ **ВАЖНО**: API ключ показывается только один раз! Сохраните его надежно.

---

## 🌐 ШАГ 5: НАСТРОЙКА ПРОКСИ (обязательно)

Для избежания блокировки IP адреса Ozon'ом нужно использовать SOCKS5 прокси.

### Где получить прокси:
- Купите у провайдера прокси (например: proxy6.net, proxy-sale.com)
- Запросите у вашего администратора сети
- Используйте VPN с поддержкой SOCKS5

### Формат прокси:
```
socks5://username:password@host:port
```

### Пример:
```
socks5://user123:pass456@185.22.134.45:1080
```

### Если прокси без авторизации:
```
socks5://185.22.134.45:1080
```

---

## ⚙️ ШАГ 6: НАСТРОЙКА .ENV ФАЙЛА

Откройте файл `.env` в текстовом редакторе (Блокнот) и заполните:

```env
# API ключи Ozon (ОБЯЗАТЕЛЬНО!)
OZON_CLIENT_ID=123456
OZON_API_KEY=ваш_длинный_ключ_из_ozon

# SOCKS5 прокси (ОБЯЗАТЕЛЬНО!)
PROXY_SOCKS5=socks5://username:password@proxy.example.com:1080

# Email для уведомлений (необязательно)
EMAIL_RECIPIENT=your@email.com

# Telegram (необязательно)
TELEGRAM_ENABLED=false
TELEGRAM_CHAT_ID=

# Настройки парсинга
SCHEDULE_INTERVAL_DAYS=14
DELAY_BETWEEN_REQUESTS=1
CRITICAL_PRICE_DIFF_PERCENT=20
```

### Объяснение параметров:

- `OZON_CLIENT_ID` - ID клиента из шага 4
- `OZON_API_KEY` - API ключ из шага 4
- `PROXY_SOCKS5` - ваш SOCKS5 прокси
- `EMAIL_RECIPIENT` - ваш email для получения отчетов
- `TELEGRAM_ENABLED` - включить Telegram уведомления (true/false)
- `SCHEDULE_INTERVAL_DAYS` - частота автоматического запуска (в днях)
- `DELAY_BETWEEN_REQUESTS` - задержка между запросами (в секундах)
- `CRITICAL_PRICE_DIFF_PERCENT` - процент разницы для критических товаров

---

## 📝 ШАГ 7: НАСТРОЙКА ПРОКСИ ФАЙЛА (опционально)

Если у вас несколько прокси, можете добавить их в файл `proxies.txt` (по одному на строку):

```
socks5://user:pass@proxy1.example.com:1080
socks5://user:pass@proxy2.example.com:1080
socks5://user:pass@proxy3.example.com:1080
```

Парсер будет автоматически ротировать между ними.

---

## 🚀 ШАГ 8: ЗАПУСК ПАРСЕРА

### Первый тестовый запуск:

1. Откройте командную строку в папке проекта
2. Выполните:
   ```
   python main.py
   ```
3. Выберите действие:
   - **1** - Запустить парсинг сейчас (вручную)
   - **2** - Запустить по расписанию (фоновый режим)
   - **3** - Выход

### Для первого раза рекомендуется:
- Выберите **1** (ручной запуск)
- Дождитесь завершения (может занять время в зависимости от количества товаров)
- Проверьте созданный Excel файл в папке `output/`

---

## 📊 ШАГ 9: ПРОВЕРКА РЕЗУЛЬТАТОВ

После завершения парсинга:

1. Откройте папку `output/`
2. Найдите файл `competitive_prices_YYYY-MM-DD_HH-MM-SS.xlsx`
3. Откройте его в Excel

### Что в файле:

**Лист "Результаты"** - все товары:
- ✅ Зеленые/белые - нормальные позиции
- 🔴 Красные - критические (разница > 20%)
- 🟡 Желтые - ошибки парсинга
- ⚫ Серые - только цена с картой

**Лист "Требует внимания"** - критические товары

**Лист "История"** - пока пустой (заполнится после нескольких запусков)

---

## 🔄 АВТОМАТИЧЕСКИЙ ЗАПУСК ПО РАСПИСАНИЮ

### Вариант 1: Через сам парсер (фоновый режим)

1. Запустите: `python main.py`
2. Выберите **2** (по расписанию)
3. Программа будет работать в фоне и запускаться каждые N дней
4. Для остановки нажмите `Ctrl+C`

### Вариант 2: Через Планировщик Windows (рекомендуется)

1. Откройте **Планировщик заданий Windows**
2. Создайте новую задачу:
   - Триггер: Каждые 14 дней (или как вам нужно)
   - Действие: Запустить программу
   - Программа: `C:\Python\python.exe` (путь к вашему Python)
   - Аргументы: `C:\ozon_parser\main.py`
   - Рабочая папка: `C:\ozon_parser`

---

## ⚠️ РЕШЕНИЕ ПРОБЛЕМ

### Ошибка: "No module named 'requests'"
**Решение**: Установите зависимости: `pip install -r requirements.txt`

### Ошибка: "OZON_CLIENT_ID не указан"
**Решение**: Заполните файл `.env` правильными ключами

### Ошибка: "Connection timeout"
**Решение**: Проверьте:
1. Работает ли ваш прокси
2. Правильно ли указан формат прокси в .env
3. Есть ли интернет соединение

### Товары не находятся (статус "Не найдено")
**Причины**:
1. Товара действительно нет у конкурентов
2. Неверный ISBN в вашем каталоге
3. Изменилась структура HTML Ozon (нужно обновить парсер)

**Решение**: Проверьте вручную на сайте Ozon, есть ли этот товар у других продавцов

### Ошибка: "HTTP 403" или "Капча"
**Решение**: 
1. Увеличьте задержку между запросами в .env: `DELAY_BETWEEN_REQUESTS=3`
2. Используйте больше прокси
3. Поменяйте прокси на другие

---

## 📈 РЕКОМЕНДАЦИИ ПО ИСПОЛЬЗОВАНИЮ

### Первый запуск:
1. Проверьте на небольшой выборке товаров
2. Убедитесь, что API ключи работают
3. Проверьте качество прокси

### Регулярное использование:
1. Запускайте парсер раз в 7-14 дней
2. Проверяйте логи в папке `logs/`
3. Анализируйте историю изменений
4. Корректируйте цены на основе данных

### Оптимизация:
1. Используйте качественные резидентные прокси
2. Увеличивайте задержку если получаете блокировки
3. Регулярно обновляйте парсер (структура Ozon может меняться)

---

## 🛠️ ОБСЛУЖИВАНИЕ

### Логи:
- Все логи сохраняются в папке `logs/`
- `main.log` - основной лог программы
- `ozon_api.log` - лог работы с API
- `parser.log` - лог парсинга

### История:
- Все результаты сохраняются в `history/`
- Организованы по датам
- Можно сравнивать результаты разных периодов

### Очистка:
- Периодически удаляйте старые логи (старше 30 дней)
- История автоматически очищается (настраивается в `history_manager.py`)

---

## 📞 ПОДДЕРЖКА

### Частые вопросы:

**Q: Сколько времени займет парсинг?**
A: Зависит от количества товаров. Примерно 2-5 секунд на товар. Для 1000 товаров ~ 1-2 часа.

**Q: Можно ли парсить только определенные категории?**
A: Да, отфильтруйте товары в `ozon_api.py` в методе `get_all_products_with_prices()`

**Q: Парсер не находит конкурентов, хотя они есть**
A: Скорее всего изменилась HTML структура Ozon. Нужно обновить селекторы в `parser.py`

**Q: Можно ли увеличить скорость?**
A: Да, но будьте осторожны - это может привести к блокировке. Уменьшите `DELAY_BETWEEN_REQUESTS` на свой риск.

---

## ✅ CHECKLIST ПЕРЕД ПЕРВЫМ ЗАПУСКОМ

- [ ] Python 3.8+ установлен
- [ ] Все зависимости установлены (`pip install -r requirements.txt`)
- [ ] Получены API ключи Ozon
- [ ] Файл .env заполнен (CLIENT_ID, API_KEY, PROXY)
- [ ] Прокси работает и правильно настроен
- [ ] Созданы папки: output/, history/, logs/
- [ ] Проведен тестовый запуск

---

## 🎓 ДЛЯ НОВИЧКОВ

Если вы впервые работаете с Python:

1. **Не бойтесь ошибок** - они нормальны
2. **Читайте сообщения об ошибках** - они подскажут что не так
3. **Проверяйте логи** - там вся информация о работе
4. **Начните с малого** - сначала протестируйте на 10-20 товарах
5. **Задавайте вопросы** - сохраните текст ошибки для помощи

---

**Удачного парсинга! 🚀**